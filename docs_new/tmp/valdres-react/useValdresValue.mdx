---
title: useValdresValue
sidebar:
    order: 2
---

The `useValdresValue` subscribes to and returns the value of an atom or
selector.

import { Stopwatch } from "../../../components/examples/Stopwatch"

<Stopwatch client:only />

```jsx
import { atom, useValdresValue, useValdresState } from "valdres-react"

const time = atom(0)
const running = atom(false)

const Comp = () => {
    const [time, setTime] = useValdresValue(time)
    const [isRunning, setIsRunning] = useValdresState(running)

    useEffect(() => {
        setInterval(() => {})
    }, [isRunning])

    return (
        <div>
            <button onClick={() => setIsRunning(curr => !curr)}>
                {isRunning ? "Start" : "Stop"}
            </button>
            <button
                onClick={() => {
                    setIsRunning(false)
                    setTime(0)
                }}
            >
                Reset
            </button>
            {count}
        </div>
    )
}
```
