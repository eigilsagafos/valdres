# useValdresStore

A React hook that returns the valdres store from the closest `<ValdresStore />`
or in the case of no parent `<ValdresStore />` it will return the default store.

```tsx
import { useValdresStore } from "react-valdres"

const Component = () => {
    const store = useValdresStore()
    return <div></div>
}
```
