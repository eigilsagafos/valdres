{
    "name": "valdres",
    "description": "Fast atom-based state management for JavaScript. Inspired by Recoil and Jotai.",
    "version": "1.0.0-beta.19",
    "license": "MIT",
    "author": {
        "name": "Eigil Sagafos"
    },
    "homepage": "https://valdres.dev",
    "repository": {
        "type": "git",
        "url": "git+https://github.com/eigilsagafos/valdres.git"
    },
    "type": "module",
    "exports": {
        ".": "./src/index.ts",
        "./adapter-internals/v1": "./src/adapter-internals/v1.ts"
    },
    "files": [
        "dist"
    ],
    "scripts": {
        "build": "NODE_ENV=production bun run build.ts",
        "build:types": "rm -rf dist/types && bunx tsgo && bun run ../../scripts/rewrite-declaration-imports.ts dist/types",
        "check:package": "bun run ../../scripts/check-valdres-package.ts --build",
        "typecheck:types": "bunx tsc -p tsconfig.types-test.json",
        "test": "bun test --reporter=dots",
        "test:bench": "rm -f test/performance/bench-results.ndjson && BENCH_SUITE=standard NODE_ENV=production bun test --timeout 60000 --concurrency 1 ./test/performance/*.bench.ts && BENCH_SUITE=standard NODE_ENV=production bun test --timeout 60000 --concurrency 1 ./test/performance/architecture.timing.ts",
        "test:bench:node": "rm -f test/performance/bench-results-node.ndjson && BENCH_SUITE=standard NODE_ENV=production npx vitest run --config vitest.bench.config.ts && BENCH_SUITE=standard NODE_ENV=production npx vitest run --config vitest.architecture-bench.config.ts",
        "test:bench:async": "rm -f test/performance/bench-results.ndjson && BENCH_SUITE=async NODE_ENV=production bun test --timeout 60000 --concurrency 1 ./test/performance/asyncSettlement.timing.ts",
        "test:bench:async:node": "rm -f test/performance/bench-results-node.ndjson && BENCH_SUITE=async NODE_ENV=production npx vitest run --config vitest.async-bench.config.ts",
        "test:architecture": "bun test ./src/lib/architecturePerformance.test.ts",
        "test:rewrite-guards:node": "npx vitest run --config vitest.rewrite-guards.config.ts",
        "test:memory:bun": "NODE_ENV=production bun test --timeout 120000 --concurrency 1 ./test/performance/*.memory.ts",
        "test:memory:node": "NODE_OPTIONS=--expose-gc NODE_ENV=production npx vitest run --config vitest.memory.config.ts"
    },
    "devDependencies": {
        "@arethetypeswrong/cli": "0.18.5",
        "@testing-library/react": "16.3.0",
        "jotai": "2.20.2",
        "mitata": "^1.0.34",
        "publint": "0.3.21",
        "react": "19.1.1",
        "react-dom": "19.1.1",
        "recoil": "0.7.7",
        "typescript": "5.9.2",
        "vitest": "^3.1.1",
        "zod": "^4.3.6"
    },
    "publishConfig": {
        "access": "public",
        "registry": "https://registry.npmjs.org/"
    },
    "gitHead": "53245fd1a080cdb1131f5be9056adc546d05ead4"
}
