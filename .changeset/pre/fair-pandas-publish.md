---
"valdres": patch
"valdres-react": patch
---

Fix published metadata for CommonJS `require(esm)` and legacy TypeScript
resolution, declare Node.js 22.12 or newer, and preserve Valdres's runtime
duplicate-instance guard during tree-shaking.
