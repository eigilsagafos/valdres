---
"valdres": patch
---

Reuse bounded negative cycle proofs for warm selectors with one or two prior
dependencies, including large/terminal/terminal/large rewires, while keeping the
first three proofs lookup-free and bounding speculative probe work. Extend the
aggregate inspection report for proof-memo decisions and bump its schema to
version 3.
